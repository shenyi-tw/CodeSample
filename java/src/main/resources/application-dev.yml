# application-dev.yml
logging:
  level:
    root: INFO
    com.example.demo: TRACE    # Detailed tracing of your business logic
    org.hibernate.SQL: DEBUG   # See the SQL queries
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE # See the SQL parameters